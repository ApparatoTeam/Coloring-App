define(function( require ){
    var $ = require('jquery');
    
    window.mod = {
        init : function(){
            
            $('page-viewport').find('h2').html('Let\'s get started');
            
         }
        
        
     };
    
    window.mod.init();
});